server:
  port: ${PORT:8080}

spring:
  datasource:
    url: "jdbc:mysql://mysql-3a9079f3-dsa-tracker.d.aivencloud.com:17740/defaultdb?useSSL=true&requireSSL=true"
    username: "avnadmin"
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect